import SwiftUI

/// Kept for Xcode template compatibility; the demo launches `DemoHomeView`.
struct ContentView: View {
    var body: some View {
        DemoHomeView()
    }
}

#Preview {
    ContentView()
}
